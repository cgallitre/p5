<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/all.min.js"></script>
    <title>Administration</title>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center m-0">
            <div class="col-auto">
                <div class="bg-light p-3 mt-5 rounded">
                    <h2 class="h3 mt-3 mb-5">
                        <i class="fas fa-lock mr-3"></i>
                        Administration
                    </h2>
                    {% if hasError %}
                    <div class="alert alert-danger">Erreur de connexion : l'email ou le mot de passe est incorrect.
                    </div>
                    {% endif %}
                    <form method="post">
                        <div class="form-group">                        
                            <input type="text" class="form-control" name="_username" value="{{ username  }}" placeholder="Saisir votre email">
                        </div>
                        <div class="form-group">
                            <input type="password" class="form-control" name="_password" placeholder="Saisir votre mot de passe">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-primary my-3" type="submit">
                                <i class="fas fa-lock-open"></i>
                                Se connecter
                            </button>
                            <a href="{{ path('homepage') }}" class="btn btn-link float-right my-3">
                                <i class="fas fa-arrow-circle-left"></i>
                                Retour au site</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>

</html>