{% extends 'admin/base.html.twig' %}

{% block title %}
    Ajout d'un message
{% endblock %}

{% block body %}
<div class="container">
    {% include 'admin/partials/titlePage.html.twig' with { 'titlePage' : 'Ajouter un message' } %}
    
    {{ form_start(form) }}

    {{ form_widget(form) }}
    <p class="text-center">
        <button type="submit" class="btn btn-primary">Ajouter le message</button>
    </p>

    {{ form_end(form) }}
</div>
{% endblock %}
