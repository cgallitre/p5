{{ form_start(form) }}

<div class="row">
    <div class="col-md-8 mx-auto">
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        {{ form_row(form.finished) }}
    </div>
</div>

<p class="text-center">
    <button type="submit" class="btn btn-primary">{{ btnForm }} le projet</button>
</p>

{{ form_end(form) }}