{% extends 'admin/base.html.twig' %}

{% block title %}
    Ajout d'un projet
{% endblock %}

{% block body %}
    <div class="container">
        {% include 'admin/partials/titlePage.html.twig' with {'titlePage' : 'Ajouter un projet' } %}

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <p class="text-center">
            <button type="submit" class="btn btn-primary">Ajouter le projet</button>
        </p>

        {{ form_end(form) }}

</div>
{% endblock %}