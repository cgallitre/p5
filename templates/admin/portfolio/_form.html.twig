{{ form_start(form) }}

<div class="row">
    <div class="col-md-6">
        {{ form_row(form.title) }}
        {{ form_row(form.description) }}
        {{ form_row(form.imageFile) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.technology) }}
        {{ form_row(form.url) }}
        {{ form_row(form.category) }}
    </div>
</div>

<p class="text-center">
    <button type="submit" class="btn btn-primary mt-3">{{ btnForm }} la référence</button>
</p>

{{ form_end(form) }}