{% extends 'admin/base.html.twig' %}

{% block title %}
    Ajout d'un thème
{% endblock %}

{% block body %}
    <div class="container">
        {% include 'admin/partials/titlePage.html.twig' with {'titlePage' : 'Ajouter un thème' } %}

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <p class="text-center">
            <button type="submit" class="btn btn-primary">Ajouter le thème</button>
        </p>

        {{ form_end(form) }}

</div>
{% endblock %}