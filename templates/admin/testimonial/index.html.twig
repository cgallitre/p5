{% extends 'admin/base.html.twig' %}

{% block title %}Témoignages{% endblock %}

{% block body %}
<div class="container">

    {% include 'admin/partials/titlePage.html.twig' with {'titlePage' : 'Témoignages' } %}
    <p class="text-center">
        <a href="{{ path('testimonial_new') }}" class="btn btn-primary">Ajouter un témoignage</a>
    </p>

    <p class="text-center mb-5">Les témoignages en couleur ne sont pas encore publiés.</p>
    <div class="table-responsive">
        <table class="table table-hover table-striped">
            <thead>
                <tr>
                    <th scope="col">Signature</th>
                    <th scope="col" class="d-none d-md-table-cell">Création</th>
                    <th scope="col" class="d-none d-md-table-cell">Contenu</th>
                    <th scope="col" width="100px">Commandes</th>
                </tr>
            </thead>
            <tbody>
                {% for testimonial in testimonials %}
                <tr class="table-{% if testimonial.published %}light{% else %}primary{% endif %}">
                    <td>{{ testimonial.author }}</td>
                    <td class="d-none d-md-table-cell">{{ testimonial.createdAt | date('d/m/Y') }}</td>
                    <td class="d-none d-md-table-cell">{{ testimonial.content }}</td>
                    <td>
                        <a href="{{ path('admin_testimonial_edit', { 'id' : testimonial.id }) }}">
                            <i class="fas fa-edit fa-2x"></i>
                        </a>
                        &emsp;
                        <a href="{{ path('admin_testimonial_delete', {'id' : testimonial.id }) }}">
                            <i class="fas fa-trash fa-2x"></i>
                        </a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>


{% endblock %}