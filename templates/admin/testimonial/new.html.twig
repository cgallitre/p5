{% extends 'admin/base.html.twig' %}

{% block title %}Ajout d'un témoignages{% endblock %}

{% block body %}

<div class="container">
        {% include 'admin/partials/titlePage.html.twig' with {'titlePage' : 'Transmettre un témoignage' } %}

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <p class="text-center">
                <button type="submit" class="btn btn-primary">Soumettre le témoignage</button>
        </p>

        {{ form_end(form) }}

</div>

{% endblock %}