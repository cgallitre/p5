{% extends 'base.html.twig' %}

{% block title %}Ajout d'un témoignages{% endblock %}

{% block body %}

<div class="container">
        {% include 'partials/titlePage.html.twig' with {'titlePage' : 'Transmettre un témoignage' } %}

        <div class="row">
                <div class="col-8 mx-auto">
                        {{ form_start(form) }}

                        {{ form_widget(form) }}

                        <p class="text-center">
                                <button type="submit" class="btn btn-primary mt-3">Soumettre le témoignage</button>
                        </p>
                        {{ form_end(form) }}
                </div>
        </div>
</div>
{% include 'partials/tinymce.html.twig' %}
{% endblock %}