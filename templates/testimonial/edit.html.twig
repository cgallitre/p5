{% extends 'base.html.twig' %}

{% block title %}Modification d'un témoignage{% endblock %}

{% block body %}

<div class="container">
        {% include 'partials/titlePage.html.twig' with {'titlePage' : 'Modifier un témoignage' } %}

        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">Modifier le témoignage</button>

        {{ form_end(form) }}

</div>

{% endblock %}